import { Layout } from "../../Components/Layout"

const Inventory = () => {
    return (
      <Layout>
        {/*
        si el usuario no se ha logueado entonces debe mostrar un pagina diciendo se registre para poder vender productos y un boton de registrarse o iniciar sesion
        si si esta logueado entonces no mostrar nada
        */}
        Inventory
      </Layout>
    )
  }

  export {Inventory}
